# Points at the labeller's INTERNAL server (not the public labeler port).
# Locally that's port 14832; in Railway use the labeller service's internal URL.
LABELLER_URL=http://localhost:14832
INTERNAL_API_KEY=
REDIS_URL=redis://localhost:6379
# POST_CACHE_TTL_DAYS=7
# POST_STORE_PATH=./posts.db
# JETSTREAM_ENDPOINT=
